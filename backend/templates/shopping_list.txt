Список покупок
Дата: {{ date }}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ИНГРЕДИЕНТЫ:
{% for ingredient in ingredients %}
{{ forloop.counter }}. {{ ingredient.ingredient__name|capfirst }} ({{ ingredient.ingredient__measurement_unit }}) — {{ ingredient.total_amount }}
{% endfor %}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

РЕЦЕПТЫ:
{% for item in recipes %}
• {{ item.recipe.name }} (автор: {{ item.recipe.author.username }})
  Теги: {{ item.recipe.tags.all|join:", " }}
{% endfor %}
